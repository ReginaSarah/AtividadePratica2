#include "../include/Hash.h"
#include "../include/Multiplicacao.h"
#include <math.h>

/*
int Multiplicacao::hashFunction(int chave, int m)
{
    float A = (sqrt(5)-1) / 2;
    int key = (int) (chave * A) % 1;
    return m * key;
}*/
